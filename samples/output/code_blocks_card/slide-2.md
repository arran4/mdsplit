```javascript
// A long javascript code block
function greet(name) {
  console.log(`Hello, ${name}!`);
}

function factorial(n) {
  if (n === 0) {
    return 1;
  }
  return n * factorial(n - 1);
}

// Another long function
function fibonacci(n) {
  if (n <= 1) {
    return n;
  }
  return fibonacci(n - 1) + fibonacci(n - 2);
}

const really_long_name_for_a_variable = "so long";

function another_long_function_and_name() {
```
